export const metadata = {
  title: "Tickets",
  // description: "Welcome to Fixed - The repair shop",
};

export default function Home({ children }: { children: React.ReactNode }) {
  return <div>Homepage</div>;
}
